import { readFileSync } from 'node:fs'; import { describe, expect, it } from 'vitest'; describe('lazy routes', () => { it('uses dynamic import in router config', () => { expect(readFileSync('src/router.js','utf8')).toContain('import('); }); });