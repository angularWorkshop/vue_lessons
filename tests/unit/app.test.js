import { describe, expect, it } from 'vitest'; import { mount } from '@vue/test-utils'; import App from '../../src/App.vue'; describe('emits and slots', () => { it('renders slot text and reacts to emit', async () => { const wrapper = mount(App); expect(wrapper.text()).toContain('Delete row'); await wrapper.get('button').trigger('click'); expect(wrapper.text()).toContain('removed'); }); });