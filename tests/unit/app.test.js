import { readFileSync } from 'node:fs'; import { describe, expect, it } from 'vitest'; import { mount } from '@vue/test-utils'; import App from '../../src/App.vue';
describe('typed generic composable', () => { it('uses a generic collection helper', () => { expect(readFileSync('src/useCollection.ts','utf8')).toContain('useCollection<T>'); }); it('renders the generic state', () => { const wrapper = mount(App); expect(wrapper.text()).toContain('success'); expect(wrapper.text()).toContain('Vue Intro'); }); });